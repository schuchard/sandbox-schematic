{
  "name": "@schuchard/schematic-generator",
  "version": "1.0.0",
  "main": "src/schematics/index.js",
  "bin": "src/schematics/index.js",
  "schematics": "./src/collection.json",
  "scripts": {
    "build:schematic": "tsc -p tsconfig.json",
    "build:schematic:watch": "tsc -p tsconfig.json -watch",
    "clean": "rm -rf sandbox && mkdir sandbox",
    "clean:launch": "yarn clean && yarn launch",
    "clean:launch:all": "yarn build:schematic && yarn clean:launch",
    "launch": "schematics --dry-run=false .:generate ",
    "test": "npm run build:schematic && jasmine src/**/*.spec.js",
    "test:ci": "yarn clean:launch && yarn test:sandbox && yarn clean",
    "test:sandbox": "cd sandbox && yarn lint && yarn test && yarn e2e && yarn build",
    "link:schematic": "yarn link"
  },
  "dependencies": {
    "@angular-devkit/core": "^0.7.2",
    "@angular-devkit/schematics": "^0.7.2",
    "@angular-devkit/schematics-cli": "^0.8.1"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "~0.7.2",
    "@angular/cli": "~6.1.2",
    "@angular/compiler-cli": "^6.1.1",
    "@angular/language-service": "^6.1.1",
    "@types/jasmine": "^2.8.8",
    "@types/node": "^10.5.5",
    "codelyzer": "~4.4.2",
    "jasmine": "^3.2.0",
    "jasmine-spec-reporter": "^4.2.1",
    "ts-node": "^7.0.0",
    "tslint": "~5.11.0",
    "typescript": "~3.0.1"
  }
}
